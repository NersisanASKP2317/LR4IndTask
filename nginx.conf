server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location /api/check {
        proxy_pass http://app:5000/check;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}